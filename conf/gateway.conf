# gateway.conf - AIoT 边缘网关配置
# 注释以 # 开头，空行忽略

# TCP 监听端口
port = 9000

# UDS 路径（进程 A → B 通信）
uds_path = /tmp/gateway_data.sock

# 单次 recv 最大读取量（字节）
max_read_size = 65536

# 数据库配置
db_path = /tmp/gateway_data.db
